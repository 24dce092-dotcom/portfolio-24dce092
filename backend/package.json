{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "npm run build --prefix ../frontend && node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
